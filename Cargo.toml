[workspace]
members = [
    "sector",
    "sector-api",
    "sector-core",
    "sector-core/src/concurrency/native/sector-concurrency",
]

[profile.release]
strip = true 
panic = "abort"
lto = true
opt-level = 'z'
codegen-units = 1
